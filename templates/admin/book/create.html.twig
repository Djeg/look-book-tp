{% extends 'admin/base.html.twig' %}

{% block title "Administration - Livres" %}

{% block menu %}
    {% set active_menu = "books" %}
    {{ parent() }}
{% endblock %}

{% block content %}
    <h1 class="font-display">
        {% block pageTitle %}
            Nouveau Livre
        {% endblock %}
    </h1>
    <div class="table-head">
        <h3>
            {% block pageSubTitle %}
                Ajouter un nouveau livre
            {% endblock %}
        </h3>
        <div>
            <a href="{{ path('app_admin_book_index') }}" class="btn">
                <i class="fa-solid fa-angle-left"></i>
                &nbsp;{% block backLabel 'Retour à la liste' %}
            </a>
        </div>
    </div>

    {{ form(form) }}

{% endblock %}
